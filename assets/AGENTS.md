# Assets Instructions

`assets/`는 게임 리소스를 저장한다.

## Layout

- `assets/images/`에는 이미지 리소스를 둔다.
- `assets/fonts/`에는 폰트 리소스를 둔다.
- `assets/sounds/`에는 효과음과 배경음악 리소스를 둔다.

## Rules

- 코드에서 참조하는 asset 경로와 실제 파일 위치를 함께 관리한다.
- 리소스 파일명은 가능하면 ASCII 소문자, 숫자, 밑줄을 사용한다.
- 빈 디렉토리는 `.gitkeep`으로 유지한다.
